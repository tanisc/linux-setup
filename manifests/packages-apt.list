# One apt package name per line.
# Lines starting with # (and blank lines) are ignored.
kubuntu-desktop
git
curl
tree
vlc
rclone
# fusermount3, used by rclone mounts (rclone-onedrive.service)
fuse3
s3cmd
qgis
openconnect
gp-saml-gui

# required by panoply
default-jre

# compilers for pip packages built from source in conda envs (e.g. gdal in ds.yml)
build-essential

# winapps deps (https://github.com/winapps-org/winapps)
dialog
freerdp3-x11
iproute2
libnotify-bin
netcat-openbsd
